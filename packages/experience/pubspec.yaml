name: experience
description: Experience-tier model and the ParallaxScene primitive for Việt Sử's immersive layer.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  # Gyro/accelerometer tilt for the flagship parallax input.
  sensors_plus: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
