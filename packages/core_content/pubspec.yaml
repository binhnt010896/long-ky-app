name: core_content
description: Content loader for Việt Sử — reads content/eras/*.json into core_domain models, with an OTA seam for later.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  core_domain:
    path: ../core_domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
